{
  "name": "@labset/buf-template-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'skipping build step for schema'",
    "lint": "./bin/lint.sh",
    "start": "modd",
    "debug": "modd -f ./modd-debug.conf"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.6.0"
  }
}
